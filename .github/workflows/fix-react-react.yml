name: Fix React React Hotfix
on:
  workflow_dispatch:
permissions:
  contents: write
jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          token: ${{ secrets.GITHUB_TOKEN }}
      - name: Apply hotfix
        run: python3 .github/fix_react_react.py index.html
      - name: Commit
        run: |
          git config user.email "noreply@anthropic.com"
          git config user.name "Claude"
          git add index.html
          git diff --staged --quiet && echo 'Nothing to commit' || (
            git commit -m "fix(fumaca): corrige React.React.createElement que quebrava a renderizacao" &&
            git push origin main
          )
